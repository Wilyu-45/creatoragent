# 多智能体协作创作应用——技术架构与MVP开发排期文档

## 第二部分：技术架构设计

### 2.1 框架选型对比与推荐

2026年，多智能体编排框架已从“百花齐放”走向收敛，LangGraph、CrewAI、AutoGen三大框架各自找到了明确的应用定位。以下是针对创作智能体场景的选型分析：

| 维度 | LangGraph | CrewAI | AutoGen (MAF) |
|---|---|---|---|
| **核心范式** | 状态图驱动的有向循环图 | 角色化团队协作 | 群聊式对话协作 |
| **编排模式** | StateGraph，支持条件分支、循环、并行 | Flows（确定性控制）+ Crews（自主协作）双层架构 | GroupChat、AgentChat，支持对话式协调 |
| **状态管理** | 内置 Typed State，支持持久化和检查点 | 支持结构化/非结构化状态，@persist() 装饰器自动持久化 | 对话历史即状态，支持人工介入 |
| **协作拓扑** | Supervisor / Hierarchical / Swarm 三种模式 | Sequential / Hierarchical / Custom 三种流程 | 群聊、双人对话、层级对话 |
| **可观测性** | LangSmith 原生集成，支持 trace replay | 内置执行追踪，支持训练模式 | AutoGen Studio 可视化 |
| **生产就绪度** | Lyft、Amazon EKS 等生产案例 | 60% Fortune 500 使用，20亿次执行 | MAF 1.0 GA，微软生态支持 |
| **上手曲线** | 中等（需理解图论概念） | 低（角色化思维模型直观） | 中等（对话式编排） |
| **适用场景** | 复杂状态机、需要精确控制流程 | 角色分工明确、快速原型 | 对话式协作、人机混合流程 |

**推荐方案：LangGraph 作为核心编排引擎 + CrewAI 作为 Agent 定义层**

理由如下：

1. **创作流程本质是有向循环图**。创作智能体的工作流（策略→创意→策划→文案→审校→核查→合规→发布）天然适合用状态图建模，LangGraph 的 StateGraph 可以精确控制每个节点的进入条件、退出条件和回退路径。腾讯云 TKE 故障自愈、Amazon EKS IT Service Desk 等生产案例均采用 LangGraph 的 StateGraph 架构，通过显式类型化状态对象获得对执行顺序和 Agent 间契约的精确控制。

2. **CrewAI 的角色化模型与创作团队高度匹配**。CrewAI 的设计哲学是将复杂任务分解为不同角色的虚拟团队——研究员、作家、审核员、项目经理——这与创作团队的角色分工完全对应。用 CrewAI 定义每个 Agent 的 Role、Goal、Backstory 和 Tools，开发效率远高于从零构建。

3. **分层使用：LangGraph 管流程，CrewAI 管角色**。LangGraph 负责全局状态机编排（阶段流转、门禁控制、返工循环），CrewAI 负责单个智能体内部的推理和工具调用。两者通过 A2A 协议或直接函数调用通信。CrewAI 的 Flows 作为确定性控制骨架，管理流程结构、状态、循环和条件执行路径，与 LangGraph 的状态图理念互补。

### 2.2 整体技术架构

系统采用 **五层架构**：

```
┌─────────────────────────────────────────────────────────────┐
│                    交互层 (Interaction Layer)                 │
│  Web UI / API Gateway / 人工审批面板 / 实时进度看板           │
├─────────────────────────────────────────────────────────────┤
│                  编排层 (Orchestration Layer)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  A0 总控      │  │  状态机引擎   │  │  门禁控制器   │       │
│  │  Orchestrator │  │  (LangGraph) │  │  (Gatekeeper) │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         └─────────────────┼─────────────────┘               │
│                           ▼                                  │
│              ┌─────────────────────────┐                     │
│              │   共享黑板 Blackboard    │                     │
│              │  (PostgreSQL + Redis)    │                     │
│              └─────────────────────────┘                     │
├─────────────────────────────────────────────────────────────┤
│                  智能体层 (Agent Layer)                       │
│  A1策略  A2创意  A3策划  A4文案  A5编辑  A6核查  A7合规     │
│  A8视觉  A9渠道  A10分析  A11记忆                            │
├─────────────────────────────────────────────────────────────┤
│                  工具与知识层 (Tool & Knowledge Layer)        │
│  RAG检索 / 搜索 / 敏感词库 / SEO工具 / 图像生成 / 数据分析   │
│  MCP Server 统一工具接入                                     │
├─────────────────────────────────────────────────────────────┤
│                  数据与基础设施层 (Data & Infra Layer)        │
│  PostgreSQL / Redis / 向量数据库 / 对象存储 / 消息队列        │
│  LangSmith / OpenTelemetry / Grafana 可观测性                 │
└─────────────────────────────────────────────────────────────┘
```

#### 2.2.1 编排层核心设计

编排层是系统的大脑，基于 LangGraph StateGraph 实现。核心设计要点：

- **状态图定义**：将创作流程建模为有向循环图，每个阶段是一个 Node，阶段之间的门禁是 Conditional Edge。返工循环通过边指向上一节点实现。
- **任务卡驱动**：A0 接收用户 Brief 后生成 TaskPacket，作为状态图的入口参数。每个 Node 消费上游 Artifact，产出自己的 Artifact 写入共享黑板。
- **门禁机制**：每个审核类 Agent（A5/A6/A7）的输出包含一个 `gate_result` 字段（`pass` / `revise` / `reject`），StateGraph 根据该字段决定流向下一节点还是回退。

#### 2.2.2 共享黑板设计

黑板是多智能体协作的核心基础设施。不使用黑板时，常见问题包括：多个 Agent 重复扫描同一目标、已排除的方向被反复尝试、候选结论被误当成已验证事实、关键发现只存在于临时消息中。

**推荐实现：PostgreSQL 持久化黑板 + Redis 实时状态**

| 状态类型 | 存储引擎 | 用途 | 示例 |
|---|---|---|---|
| Fact（事实） | PostgreSQL | 保存候选/已验证结论 | “品牌主色为#FF6B35” |
| Intent（意图） | Redis (TTL) | 声明工作方向与租约 | “A4正在撰写小红书版本” |
| Artifact（产物） | PostgreSQL + 对象存储 | 存储文案、报告等交付物 | 文案初稿v1、v2 |
| Activity（活动） | Redis | 去重与并发控制 | “A6已完成咖啡因含量核查” |
| Review（审核） | PostgreSQL | 审核记录与指令 | “A7标记‘最’字违规” |

黑板采用 **事实、意图、活动和资源四类共享状态**解决并发协作问题。每个 Agent 在开始工作前通过原子操作（SQLite事务或PostgreSQL的 `SELECT ... FOR UPDATE`）声明 Intent 并获取租约，完成后写入 Fact 并释放租约。

#### 2.2.3 MCP 工具接入层

所有外部工具通过 **MCP（Model Context Protocol）** 统一接入。MCP 解决 Agent 与工具之间的连接问题，已拥有成熟的 Python/TypeScript SDK 和几十个社区维护的 Server。创作智能体需要的 MCP Server 包括：

| MCP Server | 服务的 Agent | 功能 |
|---|---|---|
| 搜索 Server | A1, A6, A10 | 联网搜索、趋势查询 |
| RAG Server | A1-A7, A11 | 品牌知识库、历史案例检索 |
| 敏感词 Server | A7 | 广告法合规检查 |
| SEO Server | A9 | 关键词分析、排名查询 |
| 图像生成 Server | A8 | 配图、封面生成 |
| 数据分析 Server | A10 | 渠道数据回收、效果分析 |

### 2.3 智能体通信协议

系统内部 Agent 间通信采用 **A2A（Agent-to-Agent）协议**，该协议由 Google 推出，已将“Agent 之间怎么说话”标准化，获得 50 多家技术伙伴支持。

A2A 通信的核心消息格式：

```json
{
  "a2a_version": "1.0",
  "message_id": "msg_xxx",
  "from_agent": "A4",
  "to_agent": "A5",
  "intent": "handoff",
  "payload": {
    "task_id": "t_001",
    "artifact_type": "copy_draft",
    "artifact_ref": "blackboard://artifacts/t_001/copy_v1",
    "handoff_reason": "初稿完成，请求审校",
    "priority": "high"
  },
  "trace_id": "trace_abc123",
  "timestamp": "2026-09-13T10:30:00+08:00"
}
```

### 2.4 可观测性与监控

生产级多智能体系统需要覆盖 **性能指标、质量指标、成本与安全指标** 三类监控。

**技术栈推荐**：

- **分布式追踪**：OpenTelemetry + Jaeger，创建覆盖整个 Agent session 的 span，而非仅追踪单次模型调用。
- **LLM 评估**：LangSmith 或 MLflow，支持 LLM-as-a-Judge 评估、Prompt 版本管理和 trace replay。
- **业务看板**：Grafana + 自定义指标埋点。

**已落地的替代实现（as-built）**：

| 设计项 | 实际实现 | 差距 |
|---|---|---|
| OpenTelemetry 覆盖整个 Agent session 的 span | ✅ **已实现**：`app/core/tracing.py` 维护 trace/span 树（W3C trace_id / 16 位 span_id / parent / kind / attributes / status），编排层在 `graph.invoke`、每个智能体、门禁、评估、RAG 召回、发布投递、模型调用处埋点；`GET /api/tasks/{id}/trace` 提供 span 树与耗时聚合，界面「调用轨迹」为瀑布图 | 无（传播与采样见下两行） |
| OTLP 导出到 Jaeger | ✅ **已实现**：配置 `OTLP_ENDPOINT` 后经 OTel SDK 的 OTLP/HTTP exporter 转发，`docker compose up` 即带 Jaeger；**导出的 trace_id/span_id 与本地 JSON 完全一致** | 无 |
| W3C traceparent 跨进程传播 | ✅ **已实现**：入站 `POST /api/tasks` 解析 `traceparent` 头，本地 trace **沿用远端 trace_id**、首个根 span 挂到远端 span 之下（Jaeger 里拼成完整一棵树）；出站 webhook（发布投递 / 数字人网关）自动携带当前 span 的 `traceparent`；坏头一律忽略，绝不影响业务请求 | 无 |
| 采样（只作用于导出面） | ✅ **已实现**：`OTEL_TRACES_SAMPLER` / `_ARG` 与 OTel 语义对齐（`parentbased_always_on` 默认 / `parentbased_traceidratio` / `always_off`…），入站 `traceparent` 的采样标记优先于本地比例；未采样的 trace 不转发、不落盘，但**进程内轨迹始终完整**；采样计数在 `/api/metrics` 与 `/api/health` 可见 | 无 |
| Jaeger / LangSmith trace replay | 🔁 事件总线 `replay(since)` + span 树下钻（本地）＋ Jaeger UI（远端） | LangSmith 的 Prompt 版本管理与 replay 未接入 |
| Grafana 业务看板 | 🔁 `/api/metrics` + 界面「运行指标」（含 span 耗时排行） | 未接 Grafana |

**关注「哪一层最贵」而不是「哪个调用最慢」**：一次任务有 40+ 个 span，
最有价值的不是最长的那一个，而是**按层级聚合后的耗时占比**。
实测数据（Mock 引擎、返工 1 轮）：总耗时 5.7s，其中模型调用 15 次；
门禁链路（A5/A6/A7 + 评估）跨 2 轮累计 2.5s，是除编排本身外最贵的层级。
这类结论靠翻事件时间线是拼不出来的。

**关键监控指标**：

| 类别 | 指标 | 告警阈值 |
|---|---|---|
| 性能 | P99 延迟 | > 30s 触发告警 |
| 性能 | 单 Session Token 消耗 | > 50K 触发审查 |
| 质量 | 幻觉频率 | > 5% 触发人工介入 |
| 质量 | 检索精确率 | < 0.7 触发 RAG 优化 |
| 成本 | 单篇内容成本 | > 预算 120% 熔断 |
| 安全 | 合规拒绝率 | > 10% 触发词库审查 |
| 流程 | Turn Budget 命中率 | > 20% 检查 Prompt 质量 |

Turn Budget 限制每个 Session 的最大迭代轮数以防止失控成本和无限循环，行业常见上限为 25 轮后触发人工介入。

### 2.5 测试策略

多智能体系统的测试与传统软件测试有本质区别。传统单元测试失效，需要构建以 **轨迹捕获、行为不变量、回放回归、黄金数据集和 CI/CD 集成** 为核心的新型测试框架。

**推荐的三层测试体系**：

| 层级 | 测试对象 | 方法 | 工具 |
|---|---|---|---|
| 组件级 | 单个 Agent + 工具 | 固定输入→预期输出、边界条件 | pytest + Mock LLM |
| 集成级 | Agent 间交互 | 轨迹捕获、回放回归、消息契约验证 | OpenTelemetry + 自定义断言 |
| 系统级 | 端到端流程 | 黄金数据集评测、Metamorphic Testing | LangSmith Eval + RAGAS |

针对多智能体特有故障模式（容量超限、状态陈旧、Reducer 冲突等），需要设计专门的协调测试用例。例如：模拟 A5 审核不通过时的返工循环，验证 A4 能否正确接收修改意见并重新生成。

#### 2.5.1 已落地的评估闭环（替代方案）

本项目的测试体系收敛为「**可离线复现的三层脚本** + **旁路式质量评估**」，理由与实现：

| 设计目标 | 实际实现 |
|---|---|
| 组件级回归 | `scripts/doctor.py`：跑**真实智能体本体**（非生成器）验证契约漂移，覆盖向量检索、租户隔离、检查点后端、评估器复现性、回归判定器有效性、传播/采样、数字人样例共 **16 项**检查 |
| 集成级回归 | `scripts/smoke_api.py`：拉真实 uvicorn，逐条核对 `/api/*` 与 SSE 契约，含**另起带鉴权实例**验证租户隔离；`scripts/verify_contracts.py` 提供约 20 秒的快速版 |
| 系统级评测 | `scripts/stress_llm.py`：并发正确性（租约残留归零、账本不串味）+ 真实网关下的延迟/成本基线 |
| 黄金数据集 + LLM-as-a-Judge | ✅ 双轨落地：**规则评估器**（`app/core/judge.py`，可复现、即「尺子」）+ **黄金数据集**（`golden/`，固定 11 条用例 + 基线，`scripts/golden_eval.py` 判定回归）+ **模型评估器**（LLM-as-a-Judge，失败自动回退规则评估器） |

**关键设计取舍**：评估是**旁路**，不是门禁。

- 门禁（A5/A6/A7）是**否决式**的，只回答「能不能发布」；
- 评估是**度量式**的，回答「有多好、差在哪、下一轮 Prompt 往哪调」。
- 若把质量度量塞进门禁，「低于阈值就返工」会被一个有噪声的评分放大成流程抖动。
  因此默认 `JUDGE_MODE=advisory`（只打分不改裁决），需要时再切 `blocking`。

评估器**必须可失败**：`JUDGE_PROVIDER=llm` 时任何异常都静默回退离线规则评估器，
并在报告中标注 `fallback` 与原因——与 embedding 的处理原则一致，
旁路能力不能成为创作链路的新失败面。

#### 2.5.2 黄金数据集：让「改好了」变成可判定问题

固定输入是把质量讨论从主观变客观的前提。数据集与判定规则：

- **11 条用例**覆盖全部 7 个渠道 × 9 个行业，另含边界用例（极简 Brief、强监管行业、英文多语言）。
- **基线**记录质量分 / 评估分 / 返工轮次 / 产物数 / 事实准确率 / 品牌一致性 / 合规裁决序列，
  并绑定生成时的 `rubric` 与 `engine`（口径不同则拒绝直接比较）。
- **判定**：分数回退超过容差、**返工轮次增加**（更慢更贵）、监管用例首轮合规未被拦截，
  任一命中即判回归；容差内波动视作噪声。全量运行时缺用例算失败，部分运行只对范围内判定。
- **纪律**：`--update-baseline` 在存在回归时拒绝执行（需显式 `--force`），
  防止顺手把回归固化成新基线。


## 第四部分：MVP 开发排期与里程碑

### 4.1 开发策略与团队配置

参考2026年多智能体项目的典型实践，建议采用 **“一周出 MVP，一个月稳定上线”** 的节奏。核心策略是：**先交付可跑通的 59 分版本验证核心流程，再逐步加固**。

**最小开发团队配置（建议 4-6 人）** ：

| 角色 | 人数 | 核心职责 |
|---|---|---|
| 技术负责人 / Agent 架构师 | 1 | 技术选型、状态图设计、Agent 编排逻辑 |
| 后端工程师 | 1-2 | API、黑板、数据库、MCP 工具接入 |
| Prompt / 上下文工程师 | 1 | Agent 系统提示词设计、RAG 优化、评估 |
| 前端工程师 | 1 | 交互界面、审批面板、进度看板 |
| 产品经理 / 业务专家 | 0.5 | Brief 模板设计、验收标准、人工审核规则 |

### 4.2 MVP 范围定义

**MVP 阶段实现的 Agent**：A0（总控）、A1（策略）、A2（创意）、A4（文案）、A5（编辑）、A6（核查）、A7（合规）。

**MVP 核心用户故事**：
1. 用户提交创作 Brief（产品信息、目标受众、渠道、调性）。
2. 系统自动完成策略分析→创意构思→文案撰写→编辑审校→事实核查→合规审查。
3. 每个阶段产出可在界面上查看和审批。
4. 审核不通过自动回退到对应阶段。
5. 最终输出可直接使用的文案，附审核报告。

**MVP 不包含**：A3（内容策划，暂由 A2 兼任）、A8（视觉）、A9（渠道适配）、A10（分析）、A11（记忆知识库，MVP 使用简单文件存储代替）。

### 4.3 详细开发排期

#### Week 1：基础设施 + 核心链路验证（MVP Sprint）

**目标**：跑通 “Brief → 文案输出” 的最简链路，验证多智能体协作可行性。

| 天 | 任务 | 负责角色 | 交付物 |
|---|---|---|---|
| D1 | 环境搭建：LangGraph + CrewAI + PostgreSQL + Redis | 后端 | 开发环境就绪 |
| D1 | 定义 TaskPacket、AgentResult、Blackboard 数据模型 | 技术负责人 | 数据模型文档 + SQL DDL |
| D2 | 实现共享黑板（PostgreSQL 持久化 + Redis 租约） | 后端 | Blackboard SDK |
| D2 | 定义 A0 总控 StateGraph（简化版：策略→文案→审校） | 技术负责人 | 状态图定义 + 可运行代码 |
| D3 | 实现 A0 Orchestrator Agent（任务解析 + 调度） | 技术负责人 | A0 可运行 |
| D3 | 实现 A1 策略 Agent（系统提示词 + RAG 检索） | Prompt工程师 | A1 可运行 |
| D4 | 实现 A4 文案 Agent（多版本生成 + 品牌词库） | Prompt工程师 | A4 可运行 |
| D4 | 实现 A5 编辑 Agent（基础审校 + 质量评分） | Prompt工程师 | A5 可运行 |
| D5 | 端到端联调：手动输入 Brief → 输出文案 | 全员 | 可演示的 MVP 链路 |
| D5 | 基础 UI：Brief 输入 + 结果展示 | 前端 | 可操作的 Web 界面 |

**Week 1 里程碑**：用户在界面输入 Brief，系统在 3 分钟内输出一篇文案初稿和编辑审校报告。

#### Week 2：审核链路 + 门禁 + 返工循环

**目标**：加入 A6 核查、A7 合规，实现完整的审核门禁和自动返工。

| 天 | 任务 | 负责角色 | 交付物 |
|---|---|---|---|
| D6 | 实现 A6 事实核查 Agent（搜索 + RAG + 风险标记） | Prompt工程师 | A6 可运行 |
| D6 | 实现 A7 合规 Agent（敏感词库 + 品牌一致性） | Prompt工程师 | A7 可运行 |
| D7 | StateGraph 加入条件边和返工循环 | 技术负责人 | 完整状态机 |
| D7 | 门禁控制器：审核不通过 → 自动回退 → 重试计数 | 后端 | Gatekeeper 模块 |
| D8 | A2 创意总监 Agent（创意方向 + 调性指南） | Prompt工程师 | A2 可运行 |
| D8 | 人工审批节点：LangGraph interrupt + UI 审批面板 | 前端+后端 | 人工介入机制 |
| D9 | MCP 工具接入：搜索、RAG、敏感词 | 后端 | MCP Server 配置 |
| D9 | 端到端联调：完整流程含审核 + 返工 | 全员 | 可演示完整流程 |
| D10 | 错误处理 + 降级策略（工具不可用时的 fallback） | 后端 | 容错机制 |

**Week 2 里程碑**：完整流程跑通，包含策略→创意→文案→审校→核查→合规，审核不通过可自动回退，支持人工审批。

#### Week 3：可观测性 + 质量加固

**目标**：加入监控、评估和测试体系，提升系统稳定性和输出质量。

| 天 | 任务 | 负责角色 | 交付物 |
|---|---|---|---|
| D11 | OpenTelemetry + LangSmith 集成，全链路追踪 | 后端 | Trace 面板 |
| D11 | 关键指标埋点：延迟、Token 消耗、返工率、质量分 | 后端 | Grafana 看板 |
| D12 | Turn Budget + 成本熔断机制 | 后端 | 安全控制 |
| D12 | 三层测试体系搭建：组件级测试用例 | 全员 | pytest 测试套件 |
| D13 | 集成测试：轨迹捕获 + 回放回归 | 后端 | 测试报告 |
| D13 | 黄金数据集构建（10-20 个典型 Brief + 期望输出） | 产品+Prompt | 评测数据集 |
| D14 | LLM-as-a-Judge 评估流水线 | Prompt工程师 | 自动评估报告 |
| D14 | Prompt 优化迭代（基于评估结果） | Prompt工程师 | v2 系统提示词 |
| D15 | 性能优化：并发控制、黑板读写优化 | 后端 | 性能基准报告 |

**Week 3 里程碑**：系统可观测、可评估、可测试，输出质量稳定达到可接受水平。

#### Week 4：产品化 + 部署上线

**目标**：完善用户体验，部署到生产环境，准备灰度发布。

| 天 | 任务 | 负责角色 | 交付物 |
|---|---|---|---|
| D16 | UI 完善：任务列表、版本对比、审批工作流 | 前端 | 完整 UI |
| D16 | 用户反馈收集机制 | 前端+后端 | 反馈表单 |
| D17 | 数据库索引优化 + 缓存策略 | 后端 | 性能优化 |
| D17 | 安全加固：API 鉴权、数据隔离 | 后端 | 安全审计 |
| D18 | CI/CD 流水线：自动测试 + 自动部署 | 后端 | CI/CD 配置 |
| D18 | 生产环境部署（Docker + K8s 或 Serverless） | 后端 | 生产环境 |
| D19 | 灰度发布：内部团队试用 | 全员 | 试用反馈 |
| D19 | Bug 修复 + 体验优化 | 全员 | 修复清单 |
| D20 | 文档：用户手册 + 运维手册 | 全员 | 交付文档 |
| D20 | 正式上线 | 全员 | v1.0 发布 |

**Week 4 里程碑**：系统正式上线，支持多用户使用，具备完整的监控、告警和运维能力。

### 4.4 后续迭代路线

| 阶段 | 时间 | 核心内容 |
|---|---|---|
| **MVP** | Week 1-4 | A0/A1/A2/A4/A5/A6/A7 + 基础黑板 + 人工审批 |
| **v1.1** | Week 5-8 | 加入 A3 内容策划、A10 数据分析，完善 MCP 工具链 |
| **v1.2** | Week 9-12 | 加入 A8 视觉、A9 渠道 SEO，支持多平台输出 |
| **v1.3** | Week 13-16 | 加入 A11 记忆知识库，实现自动复盘和模板沉淀 |
| **v2.0** | Week 17-24 | 多语言本地化、视频脚本、A/B 测试闭环、自动发布 |

### 4.5 风险评估与应对

| 风险 | 概率 | 影响 | 应对策略 |
|---|---|---|---|
| LLM 输出质量不稳定 | 高 | 高 | 设置质量门禁 + LLM-as-a-Judge + 人工抽检 |
| 多 Agent 协作死循环 | 中 | 高 | Turn Budget 限制 + 返工次数上限 + 人工兜底 |
| 事实核查漏检 | 中 | 高 | 多重核查（搜索 + RAG + 规则）+ 高风险强制人工 |
| 合规误杀率过高 | 中 | 中 | 定期更新敏感词库 + 误杀反馈学习 |
| 开发进度延期 | 中 | 中 | MVP 范围严格收敛，非核心 Agent 延后 |
| 成本超预算 | 中 | 中 | 成本熔断 + 小模型用于审核类 Agent + 缓存 |
| 共享黑板并发冲突 | 低 | 中 | 租约机制 + 原子操作 + 冲突重试 |

### 4.6 验收标准

**功能验收**：
- 端到端流程跑通率 ≥ 95%
- 单篇内容平均生成时间 ≤ 5 分钟
- 自动返工循环正常工作，返工后质量提升率 ≥ 30%

**质量验收**：
- 事实准确率 ≥ 95%（人工抽检 100 篇）
- 合规通过率 ≥ 98%
- 品牌一致性评分 ≥ 4.5/5.0
- 初稿一次通过率 ≥ 60%

**工程验收**：
- P99 延迟 ≤ 30s
- 系统可用性 ≥ 99.5%
- 全链路 Trace 覆盖率 100%
- 单元测试覆盖率 ≥ 70%


**总结**：以上技术架构和开发排期基于 2026 年多智能体工程的最佳实践设计。核心思路是：**LangGraph 做流程编排 + CrewAI 做 Agent 定义 + PostgreSQL/Redis 做共享黑板 + MCP 做工具接入 + OpenTelemetry/LangSmith 做可观测性**。开发排期采用 4 周 MVP 节奏，第 1 周验证核心链路，第 2 周完善审核门禁，第 3 周加固质量，第 4 周上线。团队规模控制在 4-6 人，先跑通再优化。

---

## 第五部分：实施对照与进度（as-built，2026-09-13）

> 设计文档描述「目标态」，本部分描述「当前态」。逐项验证结果见 `MEMORY.md`，操作方式见 `USER_GUIDE.md`。
> 最近一轮（第五轮）补齐了：**记忆库租户隔离**、**LLM-as-a-Judge 评估流水线（D14）**，
> 并修复了「检查点静默退回内存实现导致断点续跑从未被真正覆盖」的隐患。

### 5.1 架构选型的实施取舍

| 设计（本文档） | 实际实现 | 取舍理由 |
|---|---|---|
| LangGraph 做流程编排 | ✅ 一致 | `StateGraph` + 条件边门禁环 + `interrupt()` 人工审批 + `SqliteSaver` 断点续跑 |
| CrewAI 做 Agent 定义层 | ⏸️ 未引入 | 当前 11 个智能体均为「单次结构化生成」，LangGraph 节点 + Pydantic 契约已足够；引入 CrewAI 会增加一层抽象与失败面，留待需要 Agent 内部多步推理/工具循环时再评估 |
| PostgreSQL + Redis 共享黑板 | 🔁 替代实现 | 用 JSON 文件 + 进程内锁 + `intent` 租约满足单机 MVP 的并发正确性；接口按可替换设计，后续可平滑换 PostgreSQL/Redis |
| MCP Server 统一接入 | 🔁 内置知识层 | 渠道规范 / 行业洞察 / 广告法词库 / 视觉风格库 / SEO 规则直接以 `app/knowledge/*` 提供，无外部进程依赖；外部工具（搜索/图像生成）后续按需接 MCP |
| OpenTelemetry + LangSmith | 🔁 事件总线 + SSE | `/api/metrics` 覆盖延迟 p99、成本、缓存、租约、门禁通过率、**评估分**等关键指标；全链路 Trace 待接入 |
| LangSmith Eval / RAGAS（2.5 系统级） | 🔁 自建替代 | `app/core/judge.py` 双轨评估器（规则版可复现 + 模型版失败自动回退）+ `golden/` 黄金数据集与基线回归，见 2.5.1 / 2.5.2 |
| 向量数据库 | 🔁 本地向量 + 关键词混合 | 记忆库用本地确定性 hashing embedding（可切 OpenAI `/embeddings`）+ 2-gram 关键词混合打分，零外部依赖；数据量增大后可替换为专用向量库 |
| PostgreSQL + Redis 的多租户数据隔离 | ✅ 语义已落地 | 存储仍是 JSON 文件，但**任务与记忆库都按租户分区**（鉴权令牌 → 租户，越权 404，见 creator.md 7.1） |

### 5.2 排期任务完成情况（对应 4.3）

- **Week 1（核心链路）**：✅ 完成。数据模型、共享黑板、A0 状态图、A1/A2/A3/A4/A5、端到端链路与 Web UI 均已交付。
- **Week 2（审核 + 门禁 + 返工）**：✅ 完成。A6/A7、条件边返工循环、Gatekeeper、`interrupt()` 人工审批、错误处理与降级（重试退避 → 离线引擎）均已交付。
- **Week 3（可观测性 + 质量加固）**：✅ 完成。指标埋点与 `/api/metrics`、Turn Budget、成本熔断与响应缓存、
  **LLM-as-a-Judge 评估流水线（D14）**、**黄金数据集与基线回归（D13）**、
  **调用轨迹 span 树 + OTLP 导出**、
  三层测试（`doctor.py` 组件级 + `smoke_api.py` 集成级 + `stress_llm.py` 并发/性能基线）已交付。
- **Week 4（产品化 + 部署）**：✅ 完成。UI 完善、反馈回填、**API 鉴权与租户隔离（D17）**、
  **CI 流水线（D18 前半）**、**容器化与部署（D18 后半）** 均已交付。
  仍为单副本部署（存储层未支持共享，见 §5.1 的替换路径）。

### 5.3 迭代路线完成情况（对应 4.4）

| 阶段 | 计划内容 | 状态 |
|---|---|---|
| MVP | A0/A1/A2/A4/A5/A6/A7 + 基础黑板 + 人工审批 | ✅ 完成（并已超出：A3/A8/A9/A10/A11 也已实现） |
| v1.1 | A3 内容策划、A10 数据分析 | ✅ 完成 |
| v1.2 | A8 视觉、A9 渠道 SEO、多平台输出 | ✅ 完成（多平台为「适配稿 + 排期 + webhook 投递」） |
| v1.3 | A11 记忆知识库、自动复盘与模板沉淀 | ✅ 完成（含关键词/向量混合 RAG、过期下线、**租户 + 语言隔离**、发布后复盘） |
| v2.0 | 多语言本地化、视频脚本、A/B 测试闭环、自动发布、数字人 | 🟡 **大部分完成**：多语言（中/英/日/韩/西，原生创作 + 字数口径 + 合规如实告知）✅、**视频脚本（独立交付物 + 渠道时间轴）✅**、A/B 闭环 ✅、自动发布 🟡（webhook 投递 + 到期队列）、数字人 🟡（**开发样例完成**，正式接入由使用者决定） |

**横切能力（不属任何单一版本，随需补齐）**：

| 能力 | 状态 | 说明 |
|---|---|---|
| API 鉴权 + 任务/记忆库租户隔离（D17） | ✅ 完成 | 令牌 → 租户，越权一律 404，记忆库按租户分区与计量 |
| LLM-as-a-Judge 评估流水线（D14） | ✅ 完成 | 六维评分、双评估器、按需评估、advisory/blocking 两档介入 |
| 黄金数据集与回归门禁（D13/D14） | ✅ 完成 | 固定 11 条用例（全渠道覆盖）+ 基线对比 + 返工与门禁强度回退判定 |
| CI 流水线（D18） | ✅ 完成 | `.github/workflows/ci.yml`：自检 / 质量回归 / 契约 / 端到端 / 并发 / 前端 / 文档一致性 |
| 调用轨迹（OTel 数据模型 + OTLP 导出） | ✅ 完成 | span 树覆盖整个 Agent session + 耗时排行；配 `OTLP_ENDPOINT` 即转发到 Jaeger，id 与本地一致 |
| 多语言本地化（v2.0） | ✅ 完成 | 中/英/日/韩/西语言画像；原生创作 + 按语言口径校字数 + 记忆库按语言分区 + 非中文合规如实告知 |
| 断点续跑健康度可见性 | ✅ 完成 | `/api/health.checkpointer.kind` + 自检断言，防止静默退回内存检查点 |
| 容器化与生产部署（Docker / K8s，D18 后半） | ✅ 完成 | 多阶段 Dockerfile + **受限网络变体**（`Dockerfile.offline`）+ compose（含 Jaeger）+ k8s 清单 + 无需 daemon 的清单核验；已实机验证（容器 healthy、跑通任务、`/data` 卷持久化） |
| 真实网关接入 | 🟡 进行中 | 已接 DeepSeek `deepseek-flash` 跑通并量到首版基线（单任务 ~315s / ~$0.04 / 质量分 62）；Prompt 调优待续 |
| 视频脚本（v2.0） | ✅ 完成 | 独立 `video_script` 产物：钩子 + 分镜（时长/画面/口播/字幕/机位）+ 口播表 + 字幕表 + CTA + 拍摄要点；按渠道生成时间轴 |
| 数字人（v2.0） | 🟡 开发样例完成 | **数字人渲染不在本系统内实现**（HeyGen / D-ID / 腾讯智影等授权、形象库、计费与回调协议差异极大，接哪家属产品形态决策）。系统提供可回归的接入样例 `app/core/digital_human.py`：`sample` 内置引擎（离线模拟「排队→渲染→完成」+ 按 `video_script` 生成渲染清单）与 `http` 适配样例（「POST 建任务 → GET 查状态」最小契约，未配置显式失败）；生命周期惰性推进、按租户隔离、任务删除一并回收；API + 前端面板齐备 |
| 跨进程 trace 上下文传播 / 采样 | ✅ 完成 | W3C `traceparent` 入站解析（延续远端 trace_id）+ 出站 webhook 自动携带；`OTEL_TRACES_SAMPLER` 导出面采样（OTel 语义），进程内轨迹始终完整 |

### 5.4 验收指标实测（对应 4.6）

| 指标 | 目标 | 实测 |
|---|---|---|
| 端到端流程跑通率 | ≥ 95% | 100%（`smoke_api.py` / `stress_llm.py` 全部任务到达终态） |
| 单篇生成时间 | ≤ 5 分钟 | Mock 引擎下秒级（8 任务 / 并发 4：p50 5.98s、p99 6.57s）；真实模型取决于网关延迟 |
| 返工后质量提升 | ≥ 30% | 首轮 A7 75 → 返工后 100 |
| 合规通过率 | ≥ 98% | 返工 1 轮后 100%（`doctor.py`） |
| 全链路可观测 | Trace 覆盖 100% | ✅ **span 树覆盖整个 Agent session**：实测单任务 42 个 span、3 个根（层级成型）、事件 126/127 带 trace_id；`llm.*` 全部正确嵌套在智能体/评估之下；配 `OTLP_ENDPOINT` 后 42 个 span 全部导出且 **trace_id/span_id 与本地一致**；W3C `traceparent` 跨进程传播与导出面采样已落地（入站延续远端 trace、出站 webhook 携带；采样只影响导出面，进程内轨迹完整），传播/采样/数字人样例已纳入 `doctor.py` 16 项自检与 `verify_contracts.py` |
| 内容级硬约束 | 品牌名/关键词/阻断用语/标题字数全部达标 | ✅ 11 条用例 **151 项断言全通过**（`golden_eval.py`） |
| 评估可复现性 | 同一输入同一分数 | ✅（离线评估器，`doctor.py` 断言） |
| 评估对合规风险的敏感性 | 注入阻断用语必须显著压分 | ✅ 91.0 → 46.6，合规维度 5.0 → 0.0（`doctor.py` 断言） |
| 租户隔离 | 跨租户零泄漏 | ✅ 任务越权 404、记忆库检索命中 0（`smoke_api.py` 独立鉴权实例断言） |
| 并发正确性 | 活跃租约归零 | ✅ 0 冲突 / 0 残留（`stress_llm.py -n 8 -c 4`） |
| 契约快速核验 | 新能力不静默回归 | ✅（`verify_contracts.py`，约 20s） |
| 质量回归门禁 | 改动不得让质量变差 | ✅ 11 条黄金用例 11/11 持平；判定器 9 项偏差全部正确识别（`doctor.py`） |
| 黄金用例渠道覆盖 | 全部渠道至少一条 | ✅ 7/7（`golden_eval.py --coverage`） |
| 内容级质量门禁 | 品牌名 / 关键词 / 阻断用语 / 标题字数 | ✅ 151 项断言 0 失败（曾据此发现标题压缩 off-by-one 与交付标题超限两个真实缺陷） |

> 说明：上表中延迟与成本类指标在 **Mock 引擎** 下测得；真实模型链路需按 `scripts/stress_llm.py --provider openai` 复测后再校准基线。

