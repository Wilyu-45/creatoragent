# 多智能体协作创作应用——需求基线

**读者**：研发、产品、运营与提示词工程师。**回答**：这个系统「应该是什么样」——
智能体角色定义、核心数据契约、协作流程与状态机、权限与门禁设计。
**不负责**：架构选型取舍与落地对照（见 `plan.md`）、使用操作与配置（见 `USER_GUIDE.md`）、
工程约定与历史教训（见 `ENGINEERING_PRINCIPLES.md`）。

---

## 1. 文档目标

本文档定义各智能体的职责、输入输出、工具权限、协作关系、验收指标与升级机制，
使研发、产品、运营与提示词工程师可以据此实现一个可编排、可追溯、可审核、可复盘的创作智能体系统。

适用场景：品牌文案、广告创意、新媒体内容、电商详情页、视频脚本、SEO 内容、公关稿等。

---

## 2. 总体架构

采用 **编排式多智能体 + 共享黑板 + 阶段门禁** 架构。

- **交互层**：用户输入 Brief、上传资料、人工审批。
- **编排层**：Orchestrator Agent（A0）负责拆解任务、调度智能体、控制流程。
- **智能体层**：策略、创意、策划、文案、编辑、核查、合规、视觉、渠道、数据、记忆等 Agent。
- **工具与知识层**：搜索、RAG 知识库、品牌词库、敏感词库、SEO 工具、数据分析工具。
- **数据与记忆层**：项目记忆、品牌记忆、用户偏好、历史爆款、审核记录。
- **监控层**：质量评分、事实准确率、合规通过率、交付时效、成本。

核心原则：

1. 每个智能体只做自己职责内的事。
2. 所有输出结构化，带置信度、依据和风险提示。
3. 审核类智能体有否决权，但可被人工覆盖。
4. 关键阶段设置门禁：策略未过不创作，合规未过不发布。
5. 全过程可追溯：谁在什么阶段生成了什么版本、依据是什么。

---

## 3. 智能体清单总览

| ID | 智能体 | 对应团队职位 | 核心作用 | 主要输出 |
|---|---|---|---|---|
| A0 | Orchestrator Agent | 项目经理/总控 | 拆解任务、调度、控制流程 | 任务卡、流程状态、最终交付 |
| A1 | Strategy & Insight Agent | 策略总监/用户研究 | 受众洞察、传播策略、内容目标 | 策略简报、用户画像、核心信息 |
| A2 | Creative Director Agent | 创意总监 | 定创意方向、调性、Big Idea | 创意概念、调性指南、参考方向 |
| A3 | Content Planner Agent | 内容策划/选题 | 选题、内容框架、渠道适配 | 选题清单、内容大纲、排期建议 |
| A4 | Copywriter Agent | 文案/撰稿人 | 撰写初稿、多版本文案 | 标题、正文、脚本、CTA |
| A5 | Editor Agent | 编辑/校对/主编 | 润色、校对、结构优化 | 修订稿、修改说明、质量评分 |
| A6 | Fact-Checker Agent | 事实核查 | 核查数据、引用、来源 | 核查报告、风险点、修正建议 |
| A7 | Brand & Compliance Agent | 品牌/法务合规 | 品牌一致性、广告法、敏感词 | 合规报告、禁用词、修改要求 |
| A8 | Art Director Agent | 美术指导 | 视觉方向、配图、版式建议 | 视觉 Brief、配图建议、分镜 |
| A9 | Channel & SEO Agent | 运营/SEO | 渠道适配、SEO、发布策略 | 渠道版本、关键词、发布建议 |
| A10 | Analyst Agent | 数据分析 | 效果预估、复盘、优化 | 数据报告、优化建议 |
| A11 | Memory & Knowledge Agent | 知识管理 | 知识沉淀、品牌记忆、案例检索 | 知识卡片、检索结果、版本归档 |

各智能体的职责 / 输入 / 输出 / 边界见第 4 章；指标见第 8 章；MVP 范围与迭代节奏见第 9 章。

---

## 4. 各智能体详细开发定义

### A0 Orchestrator Agent：总控编排智能体

**定位**：多智能体系统的项目经理和状态机控制器。
**职责**：
- 接收用户 Brief，解析目标、受众、渠道、预算、时间、限制条件。
- 创建任务卡 TaskPacket，拆解为策略、创意、策划、创作、审核、发布、复盘阶段。
- 调度各智能体、控制串行/并行执行，管理返工循环（审核不通过时退回对应智能体）。
- 维护共享黑板 Blackboard 与所有 Artifact 版本，在关键节点请求人工审批。
**输入 / 输出**：用户需求、上传资料、人工指令、各 Agent 输出 → 任务卡、流程状态、最终交付物、变更记录。
**工具 / 边界**：工作流引擎、消息总线、状态数据库、权限系统；目标冲突、预算超限、合规高风险、连续两次审核不通过时升级人工。

### A1 Strategy & Insight Agent：策略与洞察智能体

**定位**：策略总监 + 用户研究员。
**职责**：
- 分析目标受众、痛点、场景、竞品和传播目标。
- 输出核心信息屋：品牌主张、支撑点、利益点、证据。
- 确定内容目标（曝光/互动/转化/教育/信任），给出渠道优先级和内容基调建议。
**输入 / 输出**：Brief、市场资料、用户数据、竞品信息、品牌指南 → 策略简报、用户画像、核心信息、内容 KPI 建议。
**工具 / 边界**：搜索、RAG、数据分析、问卷/评论挖掘；结论必须带依据、区分事实/假设/创意建议；输出给 A2 与 A3。

### A2 Creative Director Agent：创意总监智能体

**定位**：创意负责人，决定“怎么说才吸引人”。
**职责**：
- 基于策略提出 Big Idea、创意概念、传播主题。
- 确定调性（专业、幽默、温暖、犀利、年轻化等），给出参考案例、禁忌方向、差异化角度。
- 审核文案是否符合创意方向。
**输入 / 输出**：策略简报、品牌指南、历史案例 → 创意概念、调性指南、创意方向 2–3 版、参考案例。
**工具 / 边界**：案例库、趋势搜索、品牌 RAG；创意与合规冲突、品牌调性冲突、方向无法收敛时升级。

### A3 Content Planner Agent：内容策划智能体

**定位**：选题策划 + 内容架构师。
**职责**：
- 将创意方向拆成具体选题和内容矩阵。
- 输出标题方向、内容大纲、段落结构、CTA 设计。
- 按渠道规划内容形式（图文、短视频、长文、海报、直播口播），给出发布节奏和系列化建议。
**输入 / 输出**：策略简报、创意概念、渠道要求 → 选题清单、内容大纲、渠道适配表、排期建议。
**工具 / 边界**：SEO 工具、热点搜索、内容日历、竞品库；输出给 A4 文案创作。

### A4 Copywriter Agent：文案创作智能体

**定位**：核心撰稿人。
**职责**：
- 根据大纲撰写标题、正文、脚本、口号、CTA、产品卖点。
- 支持多版本文案：理性版、感性版、促销版、品牌版。
- 适配不同渠道语言风格：小红书、公众号、抖音、电商、官网、PR。
- 根据编辑、核查、合规意见修改。
**输入 / 输出**：内容大纲、创意调性、品牌词库、产品资料 → 初稿、多版本文案、标题备选、CTA 备选。
**工具 / 边界**：品牌 RAG、语法检查、敏感词预检、模板库；不编造事实、不擅自承诺效果、引用需标注来源；输出给 A5。

### A5 Editor Agent：编辑审校智能体

**定位**：主编 + 校对 + 质量门禁。
**职责**：
- 检查逻辑、结构、语病、错别字、标点、重复。
- 优化表达节奏、可读性、标题吸引力。
- 统一品牌语气和术语。
- 输出修改说明和质量评分，决定是否退回 A4 重写。
**输入 / 输出**：A4 初稿、品牌指南、内容大纲 → 修订稿、修改说明、质量评分、退回意见。
**工具 / 边界**：语法检查、风格指南、LLM-as-judge、Diff 工具；事实疑点、合规风险、结构性问题时升级。

### A6 Fact-Checker Agent：事实核查智能体

**定位**：事实核查员。
**职责**：
- 核查数据、时间、人物、产品参数、引用、案例真实性。
- 标记无来源、过期、矛盾或夸大的信息。
- 要求文案补充来源或改为安全表达，输出核查报告和置信度。
**输入 / 输出**：文案稿、产品资料、外部搜索、知识库 → 事实核查报告、风险点、修正建议、来源链接。
**工具 / 否决权**：搜索、RAG、数据库、权威网站白名单；高风险未修正时禁止进入发布阶段。

### A7 Brand & Compliance Agent：品牌与合规智能体

**定位**：品牌守护者 + 法务合规审核。
**职责**：
- 检查品牌调性、Logo/口号/术语使用是否规范。
- 检查广告法、敏感词、绝对化用语、医疗/金融等特殊行业限制。
- 检查版权、隐私、肖像权、竞品贬损风险，输出合规等级和强制修改项。
**输入 / 输出**：文案稿、品牌指南、敏感词库、行业法规 → 合规报告、禁用词清单、修改要求、风险等级。
**工具 / 否决权**：敏感词库、法规库、品牌 RAG、版权检测；高风险内容必须人工复核。

### A8 Art Director Agent：视觉/美术指导智能体

**定位**：美术指导 + 视觉创意。
**职责**：
- 根据创意方向给出视觉风格、色彩、构图、版式建议。
- 生成配图 Prompt、分镜脚本、封面建议。
- 检查文案与视觉是否一致，与视频脚本、海报、详情页协作。
**输入 / 输出**：创意概念、文案稿、品牌 VI → 视觉 Brief、配图 Prompt、分镜、版式建议。
**工具 / 边界**：图像生成、设计规范库、图库、视频分镜工具；与 A4、A5、A9 协作。

### A9 Channel & SEO Agent：渠道运营与 SEO 智能体

**定位**：渠道运营 + SEO + 发布适配。
**职责**：
- 根据渠道规则调整标题、标签、关键词、话题、发布时间。
- 做 SEO 关键词布局和可搜索性优化。
- 输出多平台版本（小红书、抖音、公众号、知乎、电商等），给出发布策略、A/B 测试建议。
**输入 / 输出**：终稿、渠道目标、关键词库、平台规则 → 渠道版本、关键词、标签、发布建议、A/B 方案。
**工具**：SEO 工具、平台规则库、热点工具、内容日历。

**实现说明**：除渠道适配稿外，A9 还把「渠道 × 建议时段」固化为 `publish_plan` 排期产物，
并把时段文案（如 `07:30-08:30 通勤时段`）解析为到期时间 `due_at`（当天已过则顺延次日）。
系统不内置平台私有 SDK，到点后由后台巡检或 `POST /api/publish/tick` 触发，把「投什么」
POST 给 `PUBLISH_WEBHOOK_URL`（可对接 n8n/Zapier/自建发布网关），失败按 `PUBLISH_RETRY`
指数退避；未配置 webhook 时退化为「登记发布」，保证离线可用。取舍见 `plan.md` 5.1。

### A10 Analyst Agent：数据分析与复盘智能体

**定位**：数据分析师 + 增长顾问。
**职责**：
- 发布前预估效果，发布后回收数据；分析曝光、点击、互动、转化、留存。
- 归因内容表现，输出优化建议。
- 将成功/失败案例沉淀给 A11。
**输入 / 输出**：发布数据、渠道数据、内容版本、业务目标 → 效果报告、归因分析、优化建议、下一轮 Brief 建议。
**工具**：数据分析平台、BI、A/B 测试、统计模型。

### A11 Memory & Knowledge Agent：记忆与知识库智能体

**定位**：团队知识管理 + 长期记忆。
**职责**：
- 存储品牌指南、产品资料、历史文案、爆款案例、审核记录。
- 为其他智能体提供 RAG 检索。
- 管理版本、标签、权限和过期知识；自动沉淀复盘结论和可复用模板。
**输入 / 输出**：各 Agent 输出、人工反馈、发布数据 → 检索结果、知识卡片、版本归档、模板推荐。
**工具**：向量数据库、图数据库、文档库、权限系统。

**实现说明**：知识卡片持久化在 `memory.json`（跨任务存活、`sha1(tenant|kind|title|content)`
幂等去重、每租户容量上限 500、超过 `MAX_AGE_DAYS=180` 自动过期下线）。检索为
**关键词（中文 2-gram + 英文分词）+ 向量（本地确定性 hashing embedding，`EMBEDDING_PROVIDER=openai`
时改调任意 OpenAI 兼容 `/embeddings`，远端异常自动回退本地）** 的混合打分，按
`EMBEDDING_WEIGHT`（默认 0.35）线性混合并叠加同品牌/同渠道/同行业加成；`EMBEDDING_WEIGHT=0`
即退化为纯关键词检索。A1/A2/A4 由编排层统一召回并注入上下文（`exclude_task` 避免自我召回），
A11 自身也把检索结果写入产物。卡片带 `tenant` 归属，读写、召回、列表、统计与容量计量均按
租户分区，跨租户既不召回也不可见（未启用鉴权时全部归 `default`）。详见 §7.1。

---

## 5. 协作流程与状态机

主流程：① 需求接入（用户提交 Brief）→ ② A0 解析与建卡（生成 TaskPacket）→ ③ A1 策略洞察（策略简报）→
④ A2 创意方向（创意概念）→ ⑤ A3 内容策划（选题与大纲）→ ⑥ A4 文案创作（初稿）→ ⑦ A5 编辑审校
（修订或退回）→ ⑧ A6 事实核查（数据与来源）→ ⑨ A7 品牌合规（品牌与法规）→ ⑩ A8/A9 视觉与渠道适配
（多平台版本）→ ⑪ 人工审批（关键项目必须人工确认）→ ⑫ 发布与监测（A9 发布、A10 回收数据）→
⑬ A10 复盘、A11 沉淀。

状态机：`INIT -> STRATEGY -> CREATIVE -> PLANNING -> DRAFTING -> EDITING -> FACT_CHECK -> COMPLIANCE
-> VISUAL_ADAPT -> CHANNEL_ADAPT -> APPROVAL -> PUBLISHED -> ANALYZED -> ARCHIVED`。

任何审核不通过进入 `REVISION`，并记录返工原因。

---

## 6. 核心接口与数据模型

### TaskPacket

```json
{
  "task_id": "t_001", "parent_id": null, "phase": "DRAFTING", "assigned_agent": "A4",
  "objective": "为新品咖啡写小红书推文",
  "context": { "brand": "示例品牌", "audience": "18-30岁都市白领", "channel": "小红书", "tone": "轻松、真实、有种草感" },
  "constraints": ["不得夸大功效", "必须包含购买链接"],
  "expected_output_schema": "CopyDraft", "priority": "high", "deadline": "2026-09-13T18:00:00+08:00"
}
```

### AgentResult

```json
{
  "task_id": "t_001", "agent_id": "A4", "status": "success", "summary": "已生成3版小红书文案",
  "artifacts": [ { "type": "copy", "version": 1, "content": "..." } ],
  "evidence": [], "confidence": 0.86, "risks": ["未核实咖啡因含量"],
  "handoff": { "to": "A5", "reason": "进入编辑审校" }
}
```

---

## 7. 权限与门禁设计

| 阶段 | 负责智能体 | 门禁 |
|---|---|---|
| 策略 | A1 | 策略未确认，不进入创意 |
| 创意 | A2 | 创意方向未确认，不进入策划 |
| 初稿 | A4 | 无初稿，不进入审校 |
| 编辑 | A5 | 质量分低于阈值，退回 A4 |
| 事实 | A6 | 高风险未修正，禁止发布 |
| 合规 | A7 | 合规不通过，禁止发布 |
| 发布 | A9 | 人工审批通过后发布 |
| 复盘 | A10/A11 | 无复盘不归档 |

7.1~7.8 记录各能力的**设计约定与能力边界**；落地对照与实施取舍见 `plan.md` 第 5 部分。

### 7.1 访问控制与租户隔离（已落地）

- **鉴权开关**：`CREATOR_API_TOKENS` 为空时不挂载中间件（单机零配置）；配置后
  `/api/*`（仅 `/api/health` 免鉴权）必须携带 Token，否则 `401`。
- **Token 传递**：`Authorization: Bearer <token>`、`X-API-Token: <token>`，或 `?token=`（SSE 无法自定义请求头时）。
- **租户映射**：`tok1,tok2`（均归 `default`）与 `acme:tok1,beta:tok2`（显式指定租户）；
  解析出的租户写入 `request.state.tenant` 并落到 `TaskRecord.tenant`。
- **数据隔离**：任务列表、详情、裁决、发布、回填、黑板、事件流、指标均按租户过滤，
  跨租户访问统一返回 `404`（不泄露其他租户是否存在该任务）。
- **记忆库隔离**：A11 知识卡片带 `tenant` 归属，写入、召回、列表、统计与容量计量全部按租户分区：

  | 行为 | 隔离口径 |
  |---|---|
  | 写入 | 去重键 `sha1(tenant\|kind\|title\|content)`，不同租户的同名知识各自存活 |
  | 召回 | A1/A2/A4/A11 的检索一律带 `tenant=ctx.tenant`，不跨租户复用品牌调性 |
  | 列表 / 统计 | `GET /api/memory`、`/api/memory/search` 只返回当前租户；另给 `global_total`（仅计数）用于容量感知 |
  | 容量 | `MAX_CARDS` 按**每租户**计量，避免先入库的租户挤掉后来者的名额 |

  未启用鉴权时全部归属 `default`；缺少 `tenant` 字段的旧卡片按 `default` 读取，无需迁移脚本。

### 7.2 LLM-as-a-Judge 评估流水线（已落地，plan.md 4.3 D14）

门禁回答「能不能发布」（否决式），评估回答「有多好、差在哪」（度量式）。两者混在一起会让
「低于阈值就返工」被一个有噪声的评分放大成流程抖动，因此评估做成**独立旁路**：消费已生成的
产物，产出带证据的评分报告，不改裁决。

- **两种评估器**：`offline`（默认，确定性规则、零依赖可离线、同一输入永远同一分数，用作 CI
  回归与 Prompt 改动对照）与 `llm`（走当前 OpenAI 兼容网关；**任何异常静默回退 offline** 并标注 `fallback`）。
- **六维评分（0–5，加权汇总 0–100）**：`brief_fit 0.25`｜`compliance 0.20`｜`structure 0.15`｜
  `brand_voice 0.15`｜`fact_safety 0.15`｜`appeal 0.10`。合规维度**复用 A7 的同一份词库**
  （`scan_compliance`），避免出现「A7 判定通过、评估说合规有问题」的自相矛盾。
- **三种介入方式**：`JUDGE_MODE=off` 不评估；`advisory`（默认）只产出报告与建议、不改裁决；
  `blocking` 时 `reject` 升级人工、`review` 计入返工理由。
- **口径版本**：`RUBRIC_VERSION` 随规则变更递增，跨版本对比分数前必须先对齐。
- **产出与接口**：`data/evaluations.json`（`GET /api/evaluations`）、`judge.scored` 事件、门禁事件
  `payload.judge`、`/api/metrics.system.judge` 聚合指标，以及按需评估
  `POST /api/tasks/{id}/evaluate`（改完 Prompt 或人工改稿后无需重跑流水线）。
- **生效时机**：门禁时刻（`kind=final`，可参与 blocking 裁决）与交付前各一次，两次分差本身是信号；
  `judge.weight` 决定评估分混入综合质量分的比例（默认 `0.2`，`0` 即完全不影响）。
- **回归保障**：`scripts/doctor.py` 断言可复现性与「对合规风险的敏感性」；`scripts/verify_contracts.py` 提供快速契约核验。

### 7.3 黄金数据集与回归门禁（已落地，plan.md 4.3 D13）

内容创作本身发散，换一个 Brief 或换一次采样分数就会漂移，分不清是改进还是噪声。黄金数据集
把**输入固定**、把**输出压缩成标量**，于是质量变化变成一个可判定问题。

- **数据集**：`golden/briefs/*.json`，11 条用例覆盖全部 7 个渠道 × 9 个行业，外加两个刻意设计的
  边界用例——极简 Brief（无关键词/约束/交付物，验证缺字段不崩）与强监管行业（验证合规门禁真的拦得住）。
- **基线**：`golden/baseline.json`，记录每条用例的质量分、评估分、返工轮次、产物数、事实准确率、
  品牌一致性、合规裁决序列，以及生成它时的 `rubric` 与 `engine`；后两项参与对比前的校验。
- **判定规则**（`app/core/golden.py: compare`）：

  | 情况 | 判定 |
  |---|---|
  | 运行失败 / 全量运行时有用例缺失 | `failed` / `missing` → 不通过 |
  | 质量分或评估分回退超过容差（默认 ±3） | `regressed` → 不通过 |
  | 分数未动但返工轮次 +1 | `regressed` → 不通过（更慢更贵） |
  | 监管用例首轮合规不再被拦截 | `regressed` → 不通过（门禁强度下降） |
  | 容差内的波动 | `ok`（避免把噪声当回归） |
  | 部分运行（`-n` / `--case`） | 只对范围内用例判定，并显式标注 `partial` |

- **执行方式**：`python scripts/golden_eval.py`（CLI，退出码即结论）或 Web 界面「运行时设置 →
  回归测试」；两者共用 `app/core/golden_runner.py`，避免对「什么算通过」产生分歧。
- **纪律**：`--update-baseline` 在存在回归时拒绝执行，必须显式 `--force`。
- **自检**：`doctor.py` 逐条构造偏差，确认判定器**真的会判回归**——「永远显示通过」的假门禁比没有门禁更糟。

### 7.4 持续集成（已落地）

`.github/workflows/ci.yml` 把上述验证全部接成自动化门禁。之所以能这么做，是因为所有脚本都被
设计为**无外部依赖 + 退出码即结论**（默认走内置离线引擎，无需任何密钥）：

| 关卡 | 拦什么 |
|---|---|
| `doctor.py` | 环境/依赖缺失、智能体契约漂移、租户串号、检查点静默退化、评估器失效、假门禁、追踪树退化 |
| `golden_eval.py` | 质量回归（分数回退、返工变多、监管门禁强度下降） |
| `verify_contracts.py` | 新增能力的契约回归（快速定位） |
| `smoke_api.py` | 接口/SSE 契约、错误分支、持久化、跨租户越权、span 树层级 |
| `stress_llm.py` | 并发正确性（租约残留、账本串味） |
| 前端 `typecheck` + `build` | 类型与构建回归 |
| 文档关键内容检查 | 契约变了但文档没跟（本项目最容易出现的不一致） |

### 7.5 调用轨迹与分布式追踪（已落地，plan.md 2.4）

事件流能说明「发生了什么」，但回答不了运维最关心的三个问题：**时间与花费分布在哪一层？失败
发生在哪一层？同类任务的耗时结构是否漂移？** 事件是扁平的、按时间排的；span 树是有层级、
有耗时占比的。两者互补，不能互相替代。

**实现的是 OTel 的数据模型 + 可选的真实 OTLP 导出**：

| 项 | 本实现 |
|---|---|
| trace_id / span_id | W3C 格式（32/16 位十六进制），语义与 OTel 兼容 |
| span 属性 | `kind` / `parent_span_id` / `attributes` / `status` / 嵌套，与 OTel span 同构 |
| 导出（本地） | `data/traces/<task_id>.json`，含 `otel[]` 段，可直接被下游工具消费 |
| OTLP 导出 | 配置 `OTLP_ENDPOINT` 后经 OTel SDK 的 OTLP/HTTP exporter 转发；**导出的 trace_id/span_id 与本地完全一致**，可在 Jaeger 直接查同一条 |
| 采样 | `OTEL_TRACES_SAMPLER` / `_ARG` 与 OTel 语义对齐（`parentbased_always_on` 默认等）。采样**只作用于导出面**（OTLP + 落盘），未采样的 trace 不转发、不落盘，但进程内轨迹始终完整；入站 `traceparent` 的采样标记优先于本地比例 |
| 跨进程传播 | W3C `traceparent`：入站 `POST /api/tasks` 沿用远端 trace_id、首个根 span 挂到远端 span 之下；出站 webhook 自动携带当前 span 的 `traceparent`；坏头一律忽略，绝不影响业务请求 |

**关键设计取舍**：

- **手工构造 `ReadableSpan` 投给 span processor，而不是让 SDK 生成 span**：OTel 的 span id 由 SDK
  生成，`Tracer.start_span` 不可能带着我们自己的 `span_id`，本地 trace JSON 与 Jaeger 的 id 就对不上，
  「按 id 去 Jaeger 查这一条」随之断掉；直接构造则**本地与远端看到的是同一份数据**。
- **每次 `graph.invoke` 包一个 span**：不包这一层，所有节点 span 都会成为根 span，trace 树退化成
  「一堆并列根节点」，看不出归属且 self-time 失真。
- **人工等待单独成 span** 且与执行链并列，避免把「人在犹豫」算进系统耗时。
- **降级与缓存命中记 `ok` 不记 `error`**：它们是被设计出来的正常路径，记成错误会让错误率失去意义。
- **落盘失败绝不影响任务**；事件自动带上 `trace_id` / `span_id`（在事件总线统一注入）。
- **默认零依赖**：OTel SDK 只在配置 `OTLP_ENDPOINT` 后才 import，未配置时进程内追踪自实现。

**span 树结构**（一个任务一棵树）：

```
graph.invoke#0                    ← 每次 LangGraph 调用包一层
├─ A1.strategy_brief              ← 一个智能体一次执行 = 一个 span
│   └─ llm.A1.strategy            ← kind=client，带 provider/model/token/成本/缓存/降级
├─ memory.retrieve                ← RAG 召回（带命中条数）
├─ gate.review                    ← 门禁整体，下挂 A5/A6/A7 与 judge.evaluate
└─ …
human.wait                        ← 独立根 span：人工等待不该被算成「系统很慢」
```

**接口**：`GET /api/tasks/{id}/trace`（span 树 + 按 span 名耗时聚合 + 导出信息）、
`/api/metrics.system.tracing`（跨任务 span 耗时排行）、`/api/health.otlp`（导出状态）。
**界面**：任务详情「调用轨迹」标签页（可折叠 span 树 + 瀑布图 + 耗时排行）。

### 7.6 多语言本地化（已落地，plan.md v2.0）

做的是**语言维度**，不是「翻译功能」——每个语言一份画像，创作智能体用目标语言**原生创作**：

| 项 | 实现 |
|---|---|
| 支持语言 | 中文 / 英文 / 日文 / 韩文 / 西语（`app/knowledge/language.py`） |
| 语言画像 | 本地渠道、标题口径与上限、正文长度建议、表达惯例、合规红线、度量与日期格式、广告披露要求 |
| 提示词注入 | 非中文 Brief 自动注入「本地化指令」：原生创作、当地惯例、字数口径、合规红线、披露要求 |
| 字数口径 | **英文按词、中日韩按字**。用错口径会让「标题合规」的判断失去意义 |
| 记忆库 | 按语言分区召回：英文资产不会被中文任务当语气基线复用 |
| 离线链路 | mock 引擎全链路有英文分支（策略 + 文案 + 六个支撑产物），无密钥时多语言照样可验证 |
| 合规诚实性 | 非中文**没有自动词库**：A7 明确声明「需人工复核」、写入当地红线并强制 `needs_human_review`，**不假装检查过了** |

**已知边界（如实标注）**：非中文市场**未接入可执行的法规词库**（只有中文广告法），属部署方/
法务侧事项；度量单位与日期格式只做到「画像里声明」，未做自动换算。

### 7.7 视频脚本（已落地，plan.md v2.0）

| 项 | 状态 |
|---|---|
| 视频脚本产物 | 独立 `video_script` 产物类型，含钩子 / 分镜（时长·画面·口播·字幕·机位）/ 口播表 / 字幕表 / 行动号召 / 拍摄要点 / 合规注意 |
| 是否需要脚本 | 知识层判断（`app/knowledge/video.py`）：短视频渠道（抖音/TikTok/Reels/Shorts…）、交付物点名要脚本、渠道形态含视频特征——**任一命中才产出** |
| 分镜骨架 | 按渠道时长生成时间轴（抖音 45s/9:16/5 镜、视频号 60s、B 站 120s/16:9、TikTok 30s…），每镜给出时间占比与作用 |
| 产出位置 | A8 在产出 `visual_brief` 的同时追加 `video_script`（同一智能体两项产物，不新增智能体） |
| 数字人 | **开发样例完成**（见 §7.7.1）：渲染本身不在本系统内实现，接哪家第三方服务属部署方决策 |

**为什么「是否需要」要由知识层判断而不是一律产出**：图文渠道硬塞一份视频脚本只会制造噪声，
而且会让「交付物是否符合 Brief」这件事失去可判断性。判断口径与原因都记录在产物里。
**分镜只给骨架（时间 + 作用），具体画面与文案由智能体填充**——知识层不应假装知道这次创作的具体内容。

#### 7.7.1 数字人渲染（开发样例，plan.md v2.0）

**定位先说清楚**：HeyGen / D-ID / 腾讯智影等数字人服务的授权、形象库、计费与回调协议差异极大，
「接哪家、要不要接」是产品形态决策，由使用者决定。系统提供的是一份**可回归的接入样例**
（`app/core/digital_human.py`），让联调在买任何服务之前就能发生：

| 通道 | 行为 |
|---|---|
| `sample` 内置样例引擎（默认，零依赖） | 离线确定性模拟「排队 → 渲染 → 完成」，并按 `video_script` 产物生成**渲染清单**（每镜台词 / 字幕 / 机位 / 起止时间 / 是否有口播，无口播分镜显式告警） |
| `http` 通用适配样例 | 配置 `DIGITAL_HUMAN_API_URL` 后对接「POST 建任务 → GET 查状态」最小契约的任意网关（自建渲染农场、n8n 编排均可）；未配置时**显式失败，绝不假装成功**，厂商私有协议由你自己的网关层消化 |

- **生命周期惰性推进**：不靠后台线程，状态在读取时按流逝时间（sample）或远端状态（http）计算——
  服务空转时不产生任何任务；任务删除时渲染作业一并回收。
- **接缝**：`POST /api/tasks/{id}/digital-human`（无 `video_script` → 409）、
  `GET /api/tasks/{id}/digital-human`；按租户隔离；事件与 `digitalhuman.render` span 随任务轨迹对齐。
- **为什么是样例而不是正式接入**：正式接入需要绑定一家厂商的私有协议并处理鉴权、计费与回调安全，
  属业务决策；样例把「受理 → 记账 → 异步推进 → 失败如实呈现」的骨架与回归断言
  （`doctor.py` / `verify_contracts.py`）先立起来，换厂商时只需替换网关层。

### 7.8 容器化与部署（已落地，plan.md 4.3 D18 后半）

| 产物 | 用途 |
|---|---|
| `Dockerfile` | 多阶段构建：Node 构建前端 → Python 运行时只带 `dist/` 与后端代码 |
| `.dockerignore` | 把 `data/`、`node_modules/`、`dist/` 排除出构建上下文 |
| `docker-compose.yml` | 应用 + Jaeger 一键起；命名卷持久化 `/data` |
| `deploy/k8s.yaml` | Namespace / ConfigMap / Secret / PVC / Deployment / Service / Ingress |
| `scripts/check_deploy.py` | **不需要 Docker daemon** 的清单一致性核验 |

**几处刻意的取舍**：

- **多阶段构建**：前端构建需要 Node 与 `node_modules`（数百 MB），运行时只要「Python + `dist/`」；
  中间层全部丢弃后镜像小一个量级，攻击面也随之变小（镜像里没有 npm、没有前端构建工具链）。
- **非 root 运行**（uid 10001）+ `capabilities: drop: [ALL]`：容器内不需要任何特权。
- **数据目录 `/data` 必须挂卷**：任务、黑板、记忆库、评估历史、检查点、trace 都落在这里，不挂卷则容器重建即丢。
- **k8s 单副本 + `Recreate`**：当前持久化是本地 JSON + SQLite，多副本会各写各的导致状态分裂；
  要横向扩展必须先换存储层（PostgreSQL + Redis，见 `plan.md` 5.1），因此清单里刻意不假装支持多副本。
- **就绪探针用 `/api/health`**：它免鉴权，因此即使开了 `CREATOR_API_TOKENS` 也能探活；
  `check_deploy.py` 专门断言「探针路径 = 免鉴权路径」，否则一开鉴权容器就永远不健康。
- **SSE 的 Ingress 注解**：`proxy-buffering: off` + 长 read/send timeout，否则事件「攒着不发」，界面看起来卡住。

**为什么要有 `check_deploy.py`**：Dockerfile 的 `COPY` 路径或 compose 的环境变量一旦写错，只有真正
`docker build` 才会失败，而 CI/开发机上经常没有 daemon；把「清单引用的东西是否存在、变量名是否真被
代码读取」变成静态检查，就能在没有 daemon 的环境里提前拦住这类错误。

**受限网络下的构建（`Dockerfile.offline`）**：标准镜像需从 Docker Hub 拉 `node:22-alpine` 与
`python:3.12-slim`，部分网络不可达时镜像永远构建不出来。该变体只用**本地已有的基础镜像**
（Debian + Python 3.12、Node 22）并从镜像源安装 Python 依赖（通常只 blocked Docker Hub，PyPI 是通的）。
它是**可行方案而非更优方案**（底座更大），网络恢复后应使用标准 Dockerfile；两者产出的服务行为一致。

---

## 8. 评估与监控指标

| 层级 | 指标 |
|---|---|
| 系统级 | 交付准时率、一次通过率、返工轮次、事实准确率、合规通过率、品牌一致性、人工干预率、单篇成本、业务转化提升 |
| 智能体级 | A0 交付准时率/返工次数/流程完成率/人工干预率；A1 策略采纳率/目标匹配度/内容一致性；A2 创意新颖度/品牌一致性/采纳率；A3 选题通过率/结构完整度/渠道适配度；A4 初稿通过率/修改轮次/质量评分/转化预估；A5 审校准确率/退回率/终稿质量/抽检通过率；A6 事实准确率/漏检率/来源可追溯率；A7 合规通过率/误杀率/漏检率；A8 视觉一致性/采纳率/制作效率；A9 搜索排名/点击率/互动率/发布合规率；A10 分析准确度/建议采纳率/业务提升贡献；A11 检索命中率/知识新鲜度/复用率/错误知识率 |
| 评估级（LLM-as-a-Judge，plan.md 4.3 D14） | 综合分（0–100）与裁决分布 pass/review/reject（`/api/metrics.system.judge`）；六维均分（需求契合/合规安全/结构完整/品牌语气/事实稳妥/吸引力）；评估通过率与**回退率**（模型评估不可用而回退规则评估器的比例）；跨 `RUBRIC_VERSION` 的分数对比（Prompt 回归的唯一量化依据） |

---

## 9. MVP 与迭代路线

**MVP 版本**：A0 总控 + A1 策略 + A2 创意 + A3 策划 + A4 文案 + A5 编辑 + A6 事实核查 + A7 合规 + A10 分析。
**第二阶段**：加入 A8 视觉、A9 渠道 SEO、A11 记忆知识库。
**第三阶段**：加入多语言本地化、视频脚本、数字人、自动发布、A/B 测试闭环。

---

## 10. 风险与人工介入

必须人工介入：医疗/金融/法律等强监管行业；涉及价格、承诺、功效、数据引用；品牌危机公关；
合规高风险；智能体置信度低于阈值；多智能体意见冲突且无法收敛。

建议所有智能体输出都带 `confidence`、`evidence`、`risks`、`needs_human_review`。

---

## 11. 实现状态对照

> 本节内容已并入 `plan.md` 第 5 部分（实施对照与进度 as-built）。
