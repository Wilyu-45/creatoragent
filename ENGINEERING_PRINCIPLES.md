# 工程原则（Engineering Principles）

本文件是**工程约定与历史教训的唯一归属处**。每条原则都来自本项目实际付出过
代价的教训，新成员与后续迭代以此为开发约定 —— 同类坑不踩第二次。

**读者**：开发与评审者。**回答**：这个项目怎么改才不会踩坑。**不负责**：
需求定义（见 `creator.md`）、技术方案（见 `plan.md`）、使用操作（见 `USER_GUIDE.md`）。

---

## 一、验证与门禁

1. **自检通过 ≠ 代码正确**。自检只覆盖离线引擎与知识层；契约类改动必须跑到真实
   执行路径（`smoke_api.py` 全链路）才算验证。自检要调智能体本体（`run()`），
   不能只调生成器函数 —— 上下文字段漂移只有真实路径才会暴露。
2. **关键判定逻辑本身要有测试，且测试要能证伪**。用逐条构造偏差的反向断言证明
   「判定器真的会判回归」——「永远显示通过」的门禁比没有门禁更糟。
3. **验证脚本必须强制离线**：断言的是契约与逻辑，必须秒级、可复现、零成本；
   会被本机 `.env` 带跑偏去调真实模型。量真实链路的入口单独建
   （`real_check.py`）。
4. **自己新加的检查，必须先看到「通过」和「失败」两种表现**。断言工具的参数
   顺序本身就是一类 bug 来源。
5. **更新基线要有摩擦**：存在回归/失败/缺失时拒绝执行，必须显式 `--force`。
   「认定新合格线」是有意动作，不是顺手一跑。
6. **清单类配置要有 lint**：Dockerfile 的 COPY 路径、compose/k8s 的环境变量、
   探针路径是否免鉴权，都能在无 daemon 环境下静态拦住（`check_deploy.py`）。
   探针路径免鉴权不是巧合，是必须 —— 否则开鉴权后容器被反复重启。
7. **k8s 副本数要与存储能力匹配**：本地 JSON + SQLite 就写死 `replicas: 1`，
   在清单里假装支持多副本，比不支持更危险。
8. **flaky 测试比没有测试更糟**：在收尾边界上等待条件（如 duration > 0），
   断言接受合法状态（如 OTel 的 `unset`），而不是要求理想状态。

## 二、契约与数据

9. **契约加字段时，要搜出所有手工重建该对象的地方**。漏传一个字段，新能力
   静默失效且单语言下完全不可见。
10. **断言与被断言对象必须同口径**。字符数 vs 词数、压缩前 vs 压缩后 ——
    两边都「没错」但量纲不同的 bug 最隐蔽。
11. **派生字段不能进持久化契约**：落盘与还原都以 schema（`__dataclass_fields__`）
    为准过滤，给人看的统计字段写进基线就是 TypeError。
12. **测试资产与运行期产物分离**：黄金数据集与基线放在 `data/` 之外随代码
    版本化 —— 能 review、能 diff、能追责，否则「悄悄改宽基线」是无人察觉的
    作弊路径。
13. **多租户的指纹与容量都要按租户隔离**：去重键并入租户（同租户幂等、跨租户
    各自存活）；容量上限按租户计量（否则先到者挤光名额）。
14. **评估口径要带版本号**：权重或规则一变，旧分数不可比。跨口径对比要**先拒绝**
    （`rubric_mismatch`），而不是默默给出一个差值。
15. **浮点尾数在落盘前收干净**：`90.99999999999999` 流到接口与 JSON 里，
    会在「分数是否相等」的对比里咬人。
16. **「是否产出某产物」由知识层判断，不能一律产出**；新增产物类型要同时改
    「契约 + 产出方 + 前端 + 断言」四处，少任何一处都留下不一致。

## 三、可失败设计（降级与回退）

17. **远端依赖必须「可失败」**：embedding、评估器把远端调用的**任何异常**静默
    回退到本地实现，调用方无需 try/except；回退率本身是可观测指标。
18. **静默退化必须有可见出口**：凡是「降级后仍跑得通、但能力已缺失」的路径，
    都要有一等公民状态（`checkpointer_kind` → `/api/health` → 前端告警 +
    自检断言）。
19. **降级与缓存命中记 `ok` 不记 `error`**：熔断、降级、缓存命中是被设计出来的
    正常路径 —— 一个永不为 0 的错误率等于没有错误率。
20. **度量与门禁解耦**：门禁是否决式（能不能发），评估是度量式（有多好）。
    默认 `advisory`，权重可退化为 0 与历史行为逐位一致 —— 可回滚的开关，
    而不是一次性替换。
21. **同一事实只允许一个判定来源**：评估器的合规维度直接复用 A7 词库，两侧结论
    永远同源；自己再写一遍迟早自相矛盾。
22. **观测能力绝不能影响业务链路**：trace 落盘失败只计 `exportFailures`，
    不影响任务。
23. **未配置 ≠ 失败**：离线/单机场景下 webhook 为空是常态，等价语义要有显式
    状态（`skipped` = 登记发布）。
24. **能力不足要如实告知，而不是静默放行**：合规词库只覆盖中文时，非中文任务
    明确声明「需人工复核」—— 这是产品能否被信任的区别。

## 四、追踪与可观测

25. **追踪做完要断言「根 span 数 < span 总数」**：不包 `graph.invoke`，trace 树
    会退化成平铺列表，看起来「有追踪」但对排障毫无帮助。
26. **人工等待单独成 span，与执行链并列**：把「人在犹豫」算进节点耗时，
    排障时会误读成「系统卡了」。
27. **id 一致性是排障底线**：本地与导出的 trace_id/span_id 必须一致（按本地日志
    去 Jaeger 查这一条是最基本的动作）；导出后必须复查父子关系。
28. **自建能力要如实标注边界**：是 OTel 数据模型就不说成 OTel SDK，
    避免读者误判能力范围。
29. **事件与 span 的关联在总线里统一注入**：让每个调用方自己传 `span_id`
    必然有遗漏。

## 五、断言设计与度量口径

30. **凡是「截断到 N」的逻辑，都要断言 `len(result) <= N`**：省略号占一个字符、
    off-by-one —— 不能相信表达式「看起来对」。
31. **交付物必须受渠道规则约束，不能只靠上游自觉**：装配层兜底压缩并保留
    `base_title` / `title_limited` 便于追溯；这类缺陷分数完全正常，
    只有把规则写成断言才会暴露。
32. **断言设计要避免反向激励**：调性等主观项只做记录与人工抽查，不做机器判定
    （逼着文案堆关键词与「克制」背道而驰）；关键词用「覆盖率 ≥ N」而不是
    「必须全部命中」。
33. **文本扫描必须理解语境**：免责声明不是违规宣称；小句边界的定义必须包含
    中文标点 —— 这是中文文本处理与英文最大的差异之一。
34. **多语言断言「中文残留为 0」，而不是断言「有输出」**：多语言最容易做假的
    地方是「英文 Brief 产出中文稿」。
35. **返工轮次是成本指标，必须参与回归判定**：总分没掉但多返工一轮 = 更慢更贵
    更接近上限。门禁强度下降（首轮放行）即使分数上升也要判回归。
36. **部分运行要有独立语义**：单跑一条用例时，未运行的用例不算「缺失」，
    否则等于逼人每次跑全量。

## 六、环境与平台

37. **`subprocess.PIPE` 未消费会伪装成「服务端死锁」**：管道写满后子进程卡在
    写日志上，连 faulthandler 都无法转储。拉服务端做测试把输出重定向到文件，
    或起线程持续消费。
38. **测试临时目录走 `make_temp_dir()`，并把「目录可写」当成断言而不是假设**：
    `mkdtemp` 的 0o700 目录在受限环境下会让落盘**静默失败**、SQLite **静默退化**，
    而自检依然全绿。
39. **持久化目录必须可覆盖**（`CREATOR_DATA_DIR`）：测试互不污染，
    多实例部署成为可能。
40. **端口别写死**：Windows/Hyper-V 保留成片端口区间，写死端口会出现
    「CI 通过、本机失败」的环境相关假故障。测试脚本一律动态取端口。
41. **换个底座就换了一套隐含环境**：非官方底座显式检查 PATH、关键环境变量、
    ENTRYPOINT 三项（venv 无 pip、NODE_ENV 跳过 devDependencies、ENTRYPOINT
    让 CMD 失效）。受限网络下 Docker Hub 不通可用「本地镜像做底座 + 镜像源
    装依赖」产出可用镜像，如实标注即可。
42. **被 gitignore 的构建产物，CI 必须先构建**：本地永远不复现、只在干净
    checkout 上出现的问题，先把环境差异这个变量消掉（`mv dist dist_backup`
    复现出逐字相同的失败）。
43. **真实模型的第一课**：结构化输出会被 `max_tokens` 截断（修 JSON + 透传
    `finish_reason` + 针对性放大上限重试，原样重试必然再截断一次）；计费模型
    必须覆盖提供方真实计费维度（忽略缓存命中价会高估成本数倍 → 过早熔断），
    模型名匹配长名优先。
44. **重试只认可重试错误**：400 之类的参数错误重试没有意义。
45. **服务监听地址必须可配置，容器清单要显式声明**：进程绑 `127.0.0.1` 是安全默认，
   但容器端口发布的流量 DNAT 到容器 eth0 —— 绑 loopback 时**容器内探针照常通过、
   宿主机却连不上**，自检与冒烟全绿；只有把 `HOST` 三处声明（Dockerfile / compose /
   k8s）升级为静态断言才能提前拦住（`check_deploy.py` 第 7 项）。

## 七、文档规范（Documentation Schema）

文档的**唯一目的是降低后续开发的检索成本**。凡是「写下来只是复述已有事实」
的内容，都是在给未来的读者增加干扰。

### 7.1 唯一归属（Single Home）

每条信息只在一个文档里维护；其他文档只能「一句话 + 链接」，不得复述。

| 信息类型 | 唯一归属 | 其他文档的做法 |
|---|---|---|
| 角色定义、数据契约、流程与门禁 | `creator.md` | 链接 |
| 架构选型取舍、测试策略、as-built 对照 | `plan.md` | 链接 |
| 使用操作、配置项、评估/回归/排障步骤 | `USER_GUIDE.md` | 链接 |
| 工程约定与历史教训 | `ENGINEERING_PRINCIPLES.md` | 链接 |
| 存储契约（实体 / 签名 / 不变量 / 替换映射） | `storage_contract.md` | 链接 |
| 部署方案、清单与升级流程 | `DEPLOYMENT.md` | 链接 |
| 未完成开发待办、非显然的设计决策 | `MEMORY.md` | 链接 |
| 门面、快速开始、验证命令、上线前人工事项、文档导航 | `README.md` | — |

### 7.2 必删内容（禁止入档）

- **历史流水账**：按轮次 / 日期罗列「已完成什么」—— 代码与 git log 才是事实来源
- **可复现的实测值**：分数、耗时、token、span 数、断言数 —— 跑一次脚本就有，
  写进文档必然过时，且会与真实值悄悄分叉
- **变更记录章节**：git 已承担，重复维护只会腐烂
- **重复的配置 / 接口清单**：只在唯一归属处维护一份
- **已完成的待办**：勾完即删，不留在文档里充当「进度感」

### 7.3 每份文档的必备结构

正文之前（三行内）必须写清三件事：**读者是谁 / 回答什么问题 / 不负责什么**。
`README.md` 额外必须含「文档导航」表。

### 7.4 体量上限（超限即拆或删）

行数按**物理行**（`wc -l` 口径，含空行）计。上限是该文档正当范围对应的预算，
不是「尽量写满」的额度 —— 每份都贴近上限，说明该拆分了。

| 文档 | 上限 |
|---|---|
| `README.md` | 200 行 |
| `USER_GUIDE.md` | 700 行 |
| `creator.md` | 500 行 |
| `plan.md` | 400 行 |
| `ENGINEERING_PRINCIPLES.md` | 220 行 |
| `storage_contract.md` | 250 行 |
| `DEPLOYMENT.md` | 250 行 |
| `MEMORY.md` | 250 行 |

### 7.5 更新时机

- **新增能力** → 只改唯一归属文档；`README.md` 的能力概览最多加一行
- **新增踩坑** → 提炼成原则并入 §一~§六，**不记流水账、不建编号台账**
- **必须写数字时** → 同时写明「怎么得到这个数」（如「跑 `scripts/doctor.py` 的
  输出清单」），让读者能自行复核，而不是相信一个会过期的常数
- **删代码时** → 同步删文档里对应段落；文档与代码不一致时以代码为准并立即修文档

### 7.6 实施顺序（改造既有文档时）

1. 先按 7.1 判定每段信息的唯一归属，把重复段落收敛到一处
2. 再按 7.2 删除流水账与实测值
3. 最后按 7.4 校验体量，超限继续删 —— **优先删「正确但无用」的内容**

---

## 附：使用方式

- 新功能设计评审时，对照第一~三节自查「验证路径、契约透传、降级出口」；
- 写断言前，对照第五节自查「口径、反向激励、边界状态」；
- 排查环境相关问题时，先对照第六节 —— 大多数「本机好好的」属于这一类；
- **改动任何文档前，先对照第七节** —— 判断该写进哪一份、该不该写。
